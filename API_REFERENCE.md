# Retired — moved to docs/DATABASE.md

This file has been superseded. Real, verified query patterns and the Cloudinary upload/delete reference now live in [`docs/DATABASE.md`](docs/DATABASE.md). Note: this file previously referenced `supabase.rpc('get_events_with_participant_count', ...)` and similar RPCs that were never verified to exist and are not called anywhere in the app — treat any such claim here as unreliable, which is part of why it was retired.

`docs/FUNCTIONALITY.md` is the companion doc for app features/flows. Update those two files going forward; don't recreate an API reference here.

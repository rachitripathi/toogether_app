# Retired — moved to docs/FUNCTIONALITY.md

This was a dated snapshot (2026-07-25, branch `main` @ `dbe84cd`) and went stale within two weeks as `providers/AppProvider.tsx` kept changing — several of its "not integrated" claims (e.g. chat/messaging) were already outdated by the time it was folded in. It's been superseded by [`docs/FUNCTIONALITY.md`](docs/FUNCTIONALITY.md), which is meant to be kept current rather than treated as a point-in-time snapshot — that's the whole reason this consolidation happened.

`docs/DATABASE.md` is the companion doc for schema/RLS/migrations. Update those two files going forward; don't create another dated snapshot doc.

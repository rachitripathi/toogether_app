# Retired — moved to docs/DATABASE.md and docs/FUNCTIONALITY.md

This file's content has been folded into the project's two canonical living docs: schema/RLS/migration details (the `verification_documents`/`admin_users`/`verification_reviews` tables, the `is_admin()`/`review_verification()` functions) are now in [`docs/DATABASE.md`](DATABASE.md) §3.6–§4; the flow, dev-tools gate, and admin-panel build guidance are now in [`docs/FUNCTIONALITY.md`](FUNCTIONALITY.md) §7–§8.

Going forward, update those two files — don't recreate a feature-specific verification doc.

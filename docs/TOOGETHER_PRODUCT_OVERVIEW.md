# Retired — moved to docs/FUNCTIONALITY.md

This file was written before the Supabase backend existed (it describes events/join requests/messages/profiles as "mock in-memory data," which is no longer true) and was framed as a prototype/vision doc rather than a technical one. Its still-accurate parts — the product pitch, the three core loops, the public/private location design, the safety/trust model, the category list — have been folded into [`docs/FUNCTIONALITY.md`](docs/FUNCTIONALITY.md) §0 ("Product concept"). Its stale "Current Prototype Data" and "Future work needed" sections were dropped rather than carried forward — see `FUNCTIONALITY.md`'s real/mock breakdown in §4 instead, which is kept current.

`docs/DATABASE.md` is the companion doc for schema/RLS/migrations. Update those two files going forward; don't create another product-overview doc.

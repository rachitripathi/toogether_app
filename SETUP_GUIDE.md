# Retired — moved to docs/DATABASE.md

This file has been superseded. Deployment/config details worth keeping (Supabase project config, the `delete-avatar` Edge Function deploy/secrets steps, Cloudinary config) now live in [`docs/DATABASE.md`](docs/DATABASE.md) §6–§8. This file's repeated references to a `users` table were stale — the real table is `profiles`.

`docs/FUNCTIONALITY.md` is the companion doc for app features/flows. Update those two files going forward; don't recreate a setup guide here.

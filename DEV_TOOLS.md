# Retired — moved to docs/FUNCTIONALITY.md

This file has been superseded and folded into [`docs/FUNCTIONALITY.md`](docs/FUNCTIONALITY.md) §6 ("Dev tools"), the project's single canonical functionality doc. Note: this file's location claims had drifted (e.g. a "demo verification" disclaimer banner it described in `app/new-user-verification.tsx` no longer exists there) — the dev-tools gates currently live in `app/auth.tsx` and `app/verification/index.tsx`/`liveness.tsx`, as corrected in the new doc.

`docs/DATABASE.md` is the companion doc for schema/RLS/migrations. Update those two files going forward; don't recreate a dev-tools doc here.

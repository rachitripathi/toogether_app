# Retired — moved to docs/DATABASE.md

This file, along with `SCHEMA_DOCUMENTATION.md`, `API_REFERENCE.md`, and `SETUP_GUIDE.md`, has been superseded and folded into [`docs/DATABASE.md`](docs/DATABASE.md), the project's single canonical database doc (kept current with `migrations/`). Several of its claims were stale (a `users` table that doesn't exist, `useEvents.ts`/`useProfile.ts` hooks that are dead/unused code, etc.) — don't use this file.

`docs/FUNCTIONALITY.md` is the companion doc for app features/flows. Update those two files going forward; don't recreate a doc index here.

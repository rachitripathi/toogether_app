# Retired — moved to docs/DATABASE.md

This file has been superseded and folded into [`docs/DATABASE.md`](docs/DATABASE.md), the project's single canonical database doc (kept current with `migrations/`). Its "users" table naming and several other details here were already stale (the real table is `profiles`) — don't use this file.

`docs/FUNCTIONALITY.md` is the companion doc for app features/flows. Update those two files going forward; don't recreate a schema doc here.
